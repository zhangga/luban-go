# Luban-Go 完善计划 TodoList

当前 `luban-go` 项目已经实现了基础的：`XML配置加载` -> `类型装配(多态)` -> `基础Excel数据解析(支持简单集合嵌套)` -> `Go/C#模板代码生成` -> `JSON数据导出` 的基础管线。为了打造一个完整的商业级配置表工具，我们需要逐步完成以下核心模块的建设。

## 1. 数据校验模块 (Validator) - 🟢 优先级高
在原版 Luban 中，配置表防错的核心在于 Validator。我们需要实现 `core/validator`[ 模块，包含以下校验能力：
- [x] **Ref 引用校验**：检查外键是否存在（例如 `Type="int#ref=TbItem"`，必须在加载完所有数据后二次检查对应 id 是否存在于 TbItem 中）。
- [x] **Range 范围校验**：检查数字、字符串长度是否在指定范围内（例如 `Type="int#[1,100]"`）。
- [x] **Path 资源路径校验**：检查配置的相对路径资源（如美术图片、预制体资源）是否在项目目录下真实存在。

## 2. Group / Tag 导出过滤机制 - 🟢 优先级高
原版 Luban 可以通过定义 Tag（如 `client`、`server`、`editor`）在同一个表里分别给前端和后端导出**不同的字段**和**不同的数据**。
- [x] 解析和传递 CLI 中的 Target 信息（如 `-target=client`）
- [x] 在 CodeTarget 生成阶段过滤掉不属于当前目标组的字段
- [x] 在 DataTarget 导出阶段过滤掉不属于当前目标组的字段和数据记录

## 3. 高级数据加载能力 (DataLoader) - 🟡 优先级中
目前的 `ExcelDataLoader` 是基础版，需要支持更多强大的特性：
- [x] **表头控制列支持**：解析 `##`（类型行）、`##var`（变量行）
- [x] **行列忽略标记**：实现对以 `#` 开头的列或行进行忽略解析的功能
- [x] **多行集合与嵌套 Bean**：支持 Excel 中的**多列同名**收集成 List，或者**多行**收集成 List（即纵向 List/Map 拆分展示）
- [ ] **更多数据源支持**：增加对 CSV、JSON、Lua 等格式的数据源 Loader

## 4. 多格式数据导出 (DataTarget) - 🟡 优先级中
当前仅支持 JSON 格式，需要补充：
- [x] **Binary 二进制格式**：支持紧凑的二进制结构导出，提升加载速度并减小包体
- [x] **Lua 数据导出**：支持导出为 `.lua` 的 Table 结构，便于热更游戏使用]()
- [x] **Protobuf / FlatBuffers** 等其他序列化格式导出

## 5. 更多语言的代码模板 (CodeTarget) - ⚪ 优先级低
基于现有的 `TemplateCodeTarget` 扩展更多语言支持：
- [x] TypeScript 模板和类型映射
- [x] Lua 模板和类型映射
- [x] C++ 模板和类型映射
- [x] Java 模板和类型映射

## 6. 多语言本地化 (L10N) - ⚪ 优先级低
- [x] 增加 `text` 类型的解析
- [x] 实现静态文本的键值提取、替换和多语言文件导出逻辑
